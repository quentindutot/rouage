{
  "name": "rouage",
  "type": "module",
  "private": true,
  "license": "MIT",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "test": "pnpm run -r test",
    "build": "pnpm run -r build",
    "check": "biome check --write",
    "publish": "pnpm run build && pnpm --filter='./packages/*' publish --recursive"
  },
  "devDependencies": {
    "@biomejs/biome": "~1.9.4",
    "typescript": "~5.8.3"
  }
}
